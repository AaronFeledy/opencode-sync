node_modules
dist
*.log
.DS_Store

# Bun
bun.lockb

# TypeScript build info
*.tsbuildinfo

# Local dev
.env
.env.local
*.sqlite
*.sqlite-*
*.db
*.db-*

# Local setup/bootstrap scripts that may contain secrets — keep out of the repo
setup-*.sh
bootstrap-*.sh
*.secret
secrets/
secrets.*

# Server runtime state (when running locally for dev)
server/data/

# IDE
.vscode/
.idea/
